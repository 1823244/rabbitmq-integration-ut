
&НаКлиенте
Процедура ЗагрузитьИзJson(Команда)
	
	ЗагрузитьИзJsonНаСервере();
	
КонецПроцедуры

&НаСервере
Процедура ЗагрузитьИзJsonНаСервере()

	СсылкаКонтрагент = РеквизитФормыВЗначение("Объект").ЗагрузитьИзJsonНаСервере(JsonText);
	Сообщить(НСтр("ru = '"+строка(СсылкаКонтрагент)+"'"), СтатусСообщения.БезСтатуса);

КонецПроцедуры